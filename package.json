{
  "name": "comparasuisse-tools",
  "version": "0.0.0",
  "description": "Outils de développement pour comparasuisse.ch (site statique — pas de build en prod)",
  "private": true,
  "type": "module",
  "scripts": {
    "verify": "node scripts/verify-page.mjs",
    "postinstall": "git config core.hooksPath .githooks 2>/dev/null || true"
  },
  "devDependencies": {
    "pdfjs-dist": "^6.1.200",
    "playwright-core": "^1.49.0"
  }
}
